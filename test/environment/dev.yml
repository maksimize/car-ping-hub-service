version: "2"
services:
  go:
    build:
      context: "../"
      dockerfile: "Dockerfile"
    ports:
      - 1200:1200

  redis:
    image: redis:alpine
    restart: always
    ports:
      - 6379:6379
